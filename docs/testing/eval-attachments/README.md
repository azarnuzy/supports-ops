# Eval Attachment Demo Files

These synthetic files match the Attachment inputs in `apps/api/src/evals/cases.ts`:

- `invoice-ns-10482.pdf`: PDF invoice for the summary Case.
- `payment-screenshot.png`: image showing two completed charges.
- `invoice-ns-10482.txt`: plain-text source used to generate the PDF.

The AI Agent eval receives the OCR/transcription text declared in the Case. Use the PDF, PNG, and TXT files to demonstrate the preceding Attachment upload and extraction flow. The voice-note Case currently remains a transcript-only fixture.
