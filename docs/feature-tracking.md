# Feature Tracking

Status fitur dan halaman yang dapat dicoba pada SupportOps. Perbarui dokumen
ini pada saat GitHub issue yang mengubah status fitur ditutup.

Status yang dipakai:

- **Siap dicoba** — alur utama sudah terhubung dan dapat digunakan di lingkungan lokal dengan prasyaratnya.
- **Sebagian siap** — UI atau fondasi sudah ada, tetapi alur produk belum lengkap.
- **Belum tersedia** — belum dapat dipakai sebagai fitur produk.

## Prasyarat lokal

| Kebutuhan | Dipakai untuk |
| --- | --- |
| Postgres | Seluruh data aplikasi |
| Redis dan Worker | Publish Knowledge Source serta pengiriman email Session Link |
| `OPENROUTER_API_KEY` | Embedding saat publish Knowledge Source dan Retrieval test |
| S3-compatible object storage, `MISTRAL_API_KEY`, dan `INTERNAL_WORKER_TOKEN` | Mengunggah dan membaca Attachment dari Web Widget |
| Domain pada allowlist Web Widget | Memuat widget dari website tersebut |

## Halaman Platform

| Halaman | Akses | Status | Yang dapat dicoba | Batasan saat ini |
| --- | --- | --- | --- | --- |
| `/register`, `/login` | Publik | Siap dicoba | Membuat akun Workspace, masuk, dan keluar. Panel brand desktop memakai background terkompresi; metadata, ikon, dan judul halaman Platform juga tersedia. | Background brand tidak dimuat di layar kecil. |
| `/` | Pengguna masuk | Siap dicoba | Dashboard Admin bersifat Live ([#79](https://github.com/azarnuzy/supports-ops/issues/79)): angka Workspace menyegarkan sendiri lewat polling ±20 detik — resolusi AI terpisah antara konfirmasi Customer dan Customer tidak aktif (tidak pernah digabung menjadi satu angka), Idle Closure terpisah antara Ticket yang sedang ditangani Human Agent dan yang ditinggalkan di Shared Human Queue, tingkat eskalasi ke manusia dengan penyebut yang sama, sebaran status Ticket saat ini (empat status asli `AI_HANDLING`, `ESCALATED`, `HUMAN_HANDLING`, `RESOLVED`), rincian Ticket per Channel, dan jumlah Ticket aktif per Human Agent. Dua heatmap 7 hari per jam menambah tren historis: Conversation Traffic (Ticket baru per jam pembuatan) dan Resolutions (Ticket selesai per jam, semua alasan Resolution), refresh ±60 detik, dengan badge Live dan panel rincian saat sel dipilih. Human Agent melihat tautan ke Inbox, Shared Queue, dan My Tickets. | Bucket jam memakai UTC (belum ada zona waktu per Workspace); rentang heatmap tetap 7 hari tanpa filter Channel dan tanpa ekspor; pembaruan memakai polling, bukan push SSE. |
| `/profile` | Pengguna masuk | Siap dicoba | Mengubah nama tampilan dan URL avatar. | — |
| `/settings/agents` | Admin | Siap dicoba | Membuat Human Agent dan melihat daftar Human Agent dalam Workspace. | Belum ada pengelolaan lanjutan seperti edit/nonaktifkan akun. |
| `/settings/widget` | Admin | Siap dicoba | Layout dua kolom: form (nama AI, pesan sambutan/penutup, warna, domain yang diizinkan, logo) di kolom kiri dengan uploader logo bundar (unggah PNG/JPG/SVG maks. 2MB atau hapus), Preview di kolom kanan yang mencerminkan header, avatar, dan launcher widget sungguhan serta ikut berubah langsung saat form diedit; embed snippet di bawahnya. | Pesan penutup dipakai saat Human Agent melakukan Resolution. |
| `/settings/ai` | Admin | Siap dicoba | Mengatur instructions, Claim-time Handoff Message (`{humanAgentName}`), AI Resolution Message, jeda Follow-Up, jeda Auto-Resolution, sakelar Auto-Resolution, dan batas diam Customer untuk Ticket milik manusia (default delapan jam). Pengelolaan Tool seluruhnya pindah ke `/settings/tools`. | Pesan lifecycle berlaku per AI Agent; perubahan instructions berlaku mulai Customer Message berikutnya dan tidak mengubah generation yang sudah berjalan. Timer tetap Workspace-scoped; perubahan batas Idle Closure menjadwalkan ulang Ticket manusia yang aktif. |
| `/settings/tools` | Admin | Siap dicoba | Melihat katalog Tool (System/Webhook/MCP) dengan tipe, risk, status availability, dan sakelar on/off per AI Agent; membuat, mengubah, dan menghapus Webhook Tool (metode, URL, risk, input JSON Schema, bearer token dan secret headers write-only); serta mengisi "When to use this tool" per Tool yang aktif — teks itu ditambahkan ke deskripsi Tool yang dibaca model. | Webhook Tool yang baru dibuat langsung aktif untuk AI Agent; Tool hasil discovery MCP tidak, karena satu server bisa membawa puluhan Tool sekaligus. |
| `/settings/mcp` | Admin | Siap dicoba | Menambah, mengubah, dan menghapus MCP Server remote (Streamable HTTP); Test Connection; Discover Tools manual; meninjau Tool baru/berubah dengan konfirmasi risk sebelum diaktifkan; serta mengaktifkan/menonaktifkan Tool MCP yang sudah direview. | Discovery bersifat manual (tidak terjadwal); Tool yang sudah `UNAVAILABLE` di server remote tidak bisa diaktifkan sampai muncul kembali saat discovery berikutnya. |
| `/knowledge` | Admin | Siap dicoba | Menambah Website, satu PDF hingga 25 MB, atau teks dengan Visibility Customer-Safe/Internal-Only wajib; semua masuk Processing lalu Published otomatis. Knowledge Base menampilkan daftar ringkas dengan pencarian judul/URL, filter jenis, status ingestion real-time via SSE, dan penghapusan sumber. Memilih baris membuka drawer detail (metadata, stepper ingestion, konten hasil ekstraksi) tempat Admin mengedit judul/Visibility/konten, memicu re-index otomatis saat disimpan, melakukan Retry saat gagal, atau Update source (dengan konfirmasi) untuk PDF/Website. Kegagalan pada re-index yang sudah pernah Published menampilkan pesan "previous version remains active" karena Chunk lama tetap retrievable sampai penggantian berhasil. Tombol "Test retrieval" di header membuka dialog pencarian yang hanya mencari Chunk Published, sama seperti yang dipakai AI Agent. Sumber Customer-Safe yang sudah Published dipakai AI Agent di Web Widget; Internal-Only tidak pernah dipakai untuk balasan Customer. | PDF dan Website memerlukan Worker serta konfigurasi penyimpanan/object extraction yang sesuai; Retrieval test dan publish otomatis memerlukan `OPENROUTER_API_KEY`. Belum ada cakupan pengujian browser otomatis untuk drawer/dialog; verifikasi visual dan aksesibilitas terhadap referensi dilakukan manual. |
| `/chat` | Pengguna masuk | Siap dicoba | Unified Ticket Workspace ([#43](https://github.com/azarnuzy/supports-ops/issues/43)) adalah satu-satunya antarmuka Ticket operasional, tanpa redirect atau navigasi duplikat ([#53](https://github.com/azarnuzy/supports-ops/issues/53)): scope Mine untuk Ticket sendiri dan scope Unassigned di `/chat/unassigned` untuk Shared Human Queue, keduanya dengan URL Ticket stabil. Human Agent dapat memeriksa transcript, Attachment, prioritas, kategori, Escalation Reason, dan semua status Escalation Summary sebelum melakukan Claim atomik; Claim yang kalah memberi konflik dan me-refresh scope, sedangkan Claim yang menang mempertahankan Ticket terbuka lalu membuka composer. Pada Ticket milik sendiri, Human Agent dapat meminta Suggested Reply secara eksplisit, mengirim balasan idempoten dengan Sending/Sent/Failed dan Retry, lalu Resolution `HUMAN_RESOLVED` membuat transcript read-only. Admin yang mengambil alih Ticket dapat melanjutkan balasan, meminta Suggested Reply, dan melakukan Resolution sebagai pemiliknya. Attachment JPG/PNG dapat dibuka dalam galeri; PDF/TXT dapat dipratinjau atau diunduh setelah otorisasi Ticket. Unread state kini personal per pengguna ([#50](https://github.com/azarnuzy/supports-ops/issues/50)): posisi Message terakhir yang dibaca disimpan per Ticket per pengguna, hanya Message Customer yang menambah unread, dan Ticket ditandai terbaca segera setelah detailnya tampil. Baris Ticket dan label scope Mine/Unassigned menampilkan unread count tanpa memuat transcript penuh. Ticket yang sedang dibuka berlangganan SSE per-Ticket sendiri (Message baru, status, delivery) dan me-refetch detail dari database saat event masuk, sehingga delivery ganda atau tidak berurutan tidak pernah menduplikasi atau mengacak transcript; saat koneksi terputus, indikator "Reconnecting…" tampil non-blocking dan reconnect memicu refetch state basis data terbaru. Workspace kini responsif ([#52](https://github.com/azarnuzy/supports-ops/issues/52)): di bawah `xl` hanya satu panel (daftar Ticket atau transcript) yang tampil sekaligus mengikuti Ticket yang dipilih, dengan tombol Back untuk kembali ke daftar; panel Ticket details/Attachments/Activity dibuka lewat Sheet (focus trap, restore focus, tutup dengan Escape) di lebar medium/kecil alih-alih hilang total. Filter pencarian Ticket kini tersimpan di URL sehingga tombol Back browser mengembalikan filter sebelumnya. Scope All ([#47](https://github.com/azarnuzy/supports-ops/issues/47)) menampilkan setiap Ticket yang boleh dilihat peran pengguna dengan pencarian dan filter status sendiri. Admin kini punya scope AI-Live di `/chat/ai-live` untuk memantau Ticket yang masih ditangani AI Agent, dapat melakukan Takeover langsung dari sana, dan dapat menugaskan atau menugaskan-ulang Ticket Unassigned ke Human Agent tertentu dari scope Unassigned — kemampuan yang sempat hilang saat `/tickets/queue` dibongkar ([#53](https://github.com/azarnuzy/supports-ops/issues/53)) kini sudah dibangun ulang di `/chat`. | Attachment membutuhkan object storage; unggah dan ekstraksi juga membutuhkan Worker, `MISTRAL_API_KEY`, dan `INTERNAL_WORKER_TOKEN`. Suggested Reply membutuhkan `OPENROUTER_API_KEY` dan Knowledge Source yang telah dipublish. Pengiriman retry hanya berlangsung pada proses API yang sama; belum ada antrean retry lintas-proses. Reconnect me-refetch state saat ini, bukan replay event yang terlewat satu per satu. Belum ada cakupan pengujian browser untuk disconnect/reconnect, isolasi antar pengguna, atau perilaku responsif/keyboard lintas breakpoint; cakupan saat ini adalah unit test API dan pemeriksaan manual (clamping posisi, isolasi query unread per pengguna). |
| `/gallery` | Pengguna masuk | Siap dicoba | Melihat komponen visual dan status Ticket untuk referensi desain. | Hanya galeri komponen, bukan fitur operasional. |

## Web Widget

| Kemampuan | Status | Yang dapat dicoba | Batasan saat ini |
| --- | --- | --- | --- |
| Memuat widget dari embed snippet | Siap dicoba | Pasang snippet pada domain yang diizinkan, lalu buka launcher. Header Widget menampilkan logo Workspace bila sudah diunggah di `/settings/widget`, atau ikon chat generik bila belum. | Widget ditolak dari domain di luar allowlist. Ikon pada tombol launcher tetap ikon generik terlepas dari status logo. |
| Pre-Chat dan Session | Siap dicoba | Customer mengisi nama dan email untuk memulai Session. Agent Memory ikut terbuka bersama Session, sehingga AI Agent mengingat percakapan sejak turn pertama. | Email Session Link membutuhkan Redis, Worker, dan konfigurasi layanan email. |
| Mengirim pesan Customer dan balasan AI Agent | Siap dicoba | Customer boleh membuka dengan sapaan lebih dulu: AI Agent membalas, dan pesan berikutnya diklasifikasi dengan seluruh percakapan sebelumnya, sehingga Ticket yang terbentuk berjudul masalah sebenarnya, bukan sapaannya. Pesan pertama yang memenuhi syarat membuat Ticket; AI Agent mengambil Knowledge Source Customer-Safe yang sudah dipublish lalu membalas dalam bahasa Customer. Balasan muncul bertahap melalui SSE dan input terkunci selama generasi. Saat Customer mengonfirmasi masalahnya sudah selesai, dalam bahasa apa pun, AI Agent melakukan Resolution otomatis; sekadar ucapan terima kasih tidak dianggap konfirmasi, dan bila maksud Customer tidak jelas AI Agent bertanya dulu apakah masalahnya sudah selesai. | Jika tidak ada sumber yang mendukung jawaban, generasi gagal dua kali, atau permintaan tetap ambigu setelah dua klarifikasi, Ticket dieskalasi; belum ada UI Human Agent untuk menanganinya. Membutuhkan `OPENROUTER_API_KEY`, Postgres, Redis, Worker, dan Knowledge Source Customer-Safe yang dipublish. |

## Alur support yang belum tersedia

| Fitur | Status |
| --- | --- |
| AI Agent menjawab dari Customer-Safe Knowledge | Siap dicoba di Web Widget; lihat prasyarat dan batasan di atas. |
| Escalation dan Shared Human Queue | Siap dicoba di Web Widget — minta Human Agent secara natural dalam Bahasa Indonesia atau Inggris, atau kirim pertanyaan tanpa Knowledge Source Customer-Safe yang relevan. Ticket berpindah ke `ESCALATED`, menyimpan alasan baku dan AI Activity, lalu Customer menerima acknowledgement; pesan Customer berikutnya tetap tercatat tanpa balasan AI. Memerlukan prasyarat Web Widget dan, untuk escalation berbasis knowledge, konfigurasi AI Agent. Belum ada UI Shared Human Queue, Claim, atau Takeover untuk Human Agent. |
| Claim | Siap dicoba — Human Agent dapat Claim Ticket dari Shared Human Queue; update kondisional memastikan satu Claim menang bila dua Human Agent mencoba bersamaan. Admin dapat menugaskan Ticket kepada Human Agent lain. Membutuhkan Postgres dan Redis, serta Ticket yang sudah dieskalasi. |
| Handoff dan Escalation Summary | Siap dicoba — setelah Human Agent Claim Ticket, Customer menerima pengenalan yang menyebut nama Human Agent melalui SSE, dan Escalation Summary baru dibuat dari percakapan serta AI Activity saat itu. Ringkasan muncul otomatis di `/chat`; bila pembuatan gagal, Claim tetap berlaku dan Human Agent melihat pemberitahuan untuk meninjau percakapan langsung. Membutuhkan prasyarat Claim dan `OPENROUTER_API_KEY` untuk menghasilkan ringkasan. |
| Takeover | Siap dicoba — Admin memantau Ticket yang sedang ditangani AI dari scope AI-Live di `/chat/ai-live` dan melakukan Takeover langsung dari sana ([#53](https://github.com/azarnuzy/supports-ops/issues/53)). |
| Human Agent membalas dan melakukan Resolution | Siap dicoba — Human Agent yang memiliki Ticket dapat membalas dari `/chat`; pesan langsung masuk melalui kanal SSE Web Widget yang sama dan Customer dapat membalas tanpa refresh. Resolution mengirim pesan penutup Workspace, mencatat `HUMAN_RESOLVED`, menutup Session, serta menjadikan transkrip pada Widget read-only dengan tombol untuk memulai percakapan baru. Membutuhkan prasyarat Web Widget, Postgres, dan Redis. Pengiriman outbound disimpan dengan status dan dicoba hingga tiga kali; belum ada antrean retry lintas-proses. |
| AI Copilot dan Suggested Reply | Siap dicoba — Human Agent pemilik Ticket dapat meminta Suggested Reply dari `/chat`; draft memakai Customer-Safe dan Internal-Only Knowledge, Business Tool data, konteks Ticket sebelumnya dari Customer Identity yang sama, serta percakapan saat ini. Draft masuk ke editor untuk ditinjau atau diubah; tidak pernah terkirim otomatis. Membutuhkan `OPENROUTER_API_KEY`, Postgres, Business System, dan Knowledge Source yang dipublish. Internal-Only Knowledge hanya menjadi konteks dan kutipan lima kata atau lebih dari sana ditolak. |
| AI Agent melakukan Resolution saat Customer konfirmasi | Siap dicoba di Web Widget — saat AI Agent masih menangani Ticket dan Customer memberi konfirmasi yang jelas bahwa masalahnya sudah selesai (dalam Bahasa Indonesia atau Inggris), AI Agent mengirim AI Resolution Message miliknya, mencatat `CUSTOMER_CONFIRMED` sebagai `resolvedBy` AI Agent, menutup Session, dan mencatat AI Activity `RESOLVED`. Human Resolution tetap memakai pesan penutup Workspace. Sekadar ucapan terima kasih tidak memicu Resolution; bila maksud tidak jelas AI Agent bertanya dulu. Membutuhkan `OPENROUTER_API_KEY`, Postgres, dan Redis. |
| Follow-Up dan Auto-Resolution | Siap dicoba — Admin mengatur jeda Follow-Up, jeda Auto-Resolution, dan sakelar Auto-Resolution di `/settings/ai` (nilai dalam detik agar mudah diuji lokal). Setelah balasan AI, Customer yang diam menerima satu Follow-Up yang merujuk judul Ticket; jika tetap diam setelahnya, Ticket ditutup sebagai `CUSTOMER_INACTIVE`. Pesan Customer membatalkan timer, balasan AI baru mengganti timer, dan Ticket yang di-Claim atau di-Takeover tidak pernah menjalankannya. Membutuhkan Postgres, Redis, API, Worker, serta Ticket Web Widget yang ditangani AI. Auto-Resolution yang dimatikan tetap mengizinkan Follow-Up tetapi tidak menutup Ticket. |
| Idle Closure | Siap dicoba — Admin mengatur batas diam Customer di `/settings/ai`, default delapan jam. Batas dihitung dari Customer Message terakhir untuk Ticket `HUMAN_HANDLING` maupun Ticket `ESCALATED` yang belum di-Claim; balasan Human Agent tidak menggesernya. Worker mengirim pesan penutup, menutup Session, mencatat `PLATFORM` sebagai actor, dan membedakan `CUSTOMER_INACTIVE_HUMAN_HANDLING` dari `CUSTOMER_INACTIVE_SHARED_QUEUE`. Pesan Customer atau perubahan ownership yang menang race membuat job lama batal lewat recheck transaksi. Membutuhkan Postgres, Redis, API, dan Worker. |
| Activity Timeline dan UI AI Activity | Siap dicoba — panel Ticket details di `/chat` menampilkan Activity Timeline dari AI Activity (Ticket dibuat, klasifikasi, retrieval Knowledge, panggilan/kegagalan Tool dengan nama Tool saja tanpa secret atau hasil mentah, balasan/klarifikasi AI, Escalation dengan alasannya, Follow-Up, Resolution, Claim, Takeover, Handoff, ringkasan Escalation) dalam bahasa yang terbaca, bukan JSON mentah. |
| Business Tools | Siap dicoba — Admin mengelola Tool dari `/settings/tools` (tab Tools dan MCP): katalog, sakelar on/off per AI Agent, guidance "When to use this tool", Webhook Tool CRUD, serta MCP Server CRUD dengan Test Connection, Discover, dan review risk. Orchestration AI Agent memakai resolver yang sama: seluruh Tool yang aktif ditawarkan ke model, dan model yang memilih mana yang dipanggil berdasarkan deskripsi Tool plus guidance Admin (maksimum tiga panggilan dan 15 detik per Customer Message). Tidak ada aturan yang memaksa sebuah Tool dipanggil — Tool Policy per kategori Ticket sudah dihapus (ADR-0016). Tool yang belum diaktifkan/dinonaktifkan/`UNAVAILABLE` tidak pernah bisa dipanggil walau disebut namanya oleh Customer. Dua System Tool mempertahankan batas Customer-Safe serta Customer Identity/Channel; executor Webhook dan MCP memvalidasi input dan Workspace, membatasi hasil hingga 64 KB, serta menolak target/redirect tidak aman. AI Activity mencatat setiap panggilan/kegagalan (`TOOL_CALLED`/`TOOL_FAILED`) dengan origin, risk, dan latency tanpa secret atau hasil mentah; panggilan Tool yang gagal mengeskalasi Ticket sebagai `BUSINESS_TOOL_FAILURE`. Demo end-to-end memakai `apps/business-system` sebagai HTTP API sekaligus demo MCP Server (`/mcp`, Streamable HTTP): `pnpm seed:demo` membuat Webhook Tool `getSubscriptionStatus`, MCP Server dengan Tool `getInvoiceStatus`, mengaktifkan keduanya dengan guidance masing-masing, dan menyeed empat contoh percakapan siap pakai; `pnpm seed:demo -- --reset` menyeed ulang dari nol. Lihat `docs/testing/e2e-runbook.md` skenario W3–W3c. Membutuhkan `TOOL_MASTER_KEY`; HTTP dan MCP lokal (mis. Business System demo) hanya aktif di non-production dengan `ALLOW_LOCAL_HTTP_TOOLS=true`. Batasan: runtime belum meneruskan Customer Identity ke input Tool, sehingga Tool demo menjawab dari satu akun demo tetap (`cus_102`) alih-alih akun Customer yang sedang chat. |
| Attachment | Siap dicoba di Web Widget dan workspace `/chat` — kirim PDF, plain text, JPEG, atau PNG (maks. 10 MB) bersama konteks singkat; status processing terlihat dan teks hasil ekstraksi menjadi konteks Ticket untuk balasan AI. Human Agent pemilik Ticket juga dapat mengirim hingga sepuluh file tersebut dalam satu balasan dari `/chat`; Customer menerimanya pada stream Message yang sama. Di `/chat`, gambar muncul sebagai thumbnail dan galeri Ticket, sementara PDF/TXT memiliki Preview dan Download; akses URL bertanda-tangan singkat memeriksa hak melihat Ticket induknya. Original tetap tersedia ketika ekstraksi diproses atau gagal. Membutuhkan object storage, Worker, `MISTRAL_API_KEY` (kecuali plain text), `INTERNAL_WORKER_TOKEN`, dan konfigurasi AI Agent. Outbound saat ini hanya Web Widget. |
| Telemetry AI Agent | Siap dicoba — jalankan `apps/api` dengan `ENABLE_TELEMETRY="true"`, `TELEMETRY_EXPORTER="otlp"`, endpoint dan kredensial OTLP (default Langfuse Cloud) sesuai [docs/setup/06-observability.md](setup/06-observability.md), atau `TELEMETRY_EXPORTER="console"` untuk melihat span di stdout. Satu run AI Agent menghasilkan satu trace utuh: `ai_agent.run` mencakup retrieval, tiap panggilan Business Tool, generasi model, dan keputusan akhir beserta Escalation Reason. Capture redacted (mode `safe`) — isi prompt/respons tidak pernah dikirim, sehingga materi Internal-Only tidak lolos dalam bentuk mentah; pending span di-flush saat shutdown maupun exit normal. AI Activity tetap menjadi record produk terpisah di database. |
| Eval suite AI Agent | Siap dicoba — `pnpm eval:ai-agent` (opsional `--category` dan `--case`) menjalankan Eval Case terhadap corpus tetap yang dimuat di memori, bukan terhadap Workspace nyata. Delapan kategori: visibility safety, grounding, escalation yang wajib terjadi, escalation yang tidak boleh terjadi, tool calling, classification, resolution detection, dan language; tiap kategori punya negative control yang selalu gagal. Hasil tercetak di konsol dan dilaporkan lewat OTel eval reporter yang sama dengan Telemetry AI Agent. Dijalankan manual, bukan gate CI. Membutuhkan `OPENROUTER_API_KEY`. |
| WhatsApp dan email sebagai Channel support | Belum tersedia |

## Aturan pembaruan

Saat menutup GitHub issue yang menambah, menghapus, atau mengubah kesiapan fitur:

1. Perbarui baris atau bagian terkait di dokumen ini pada perubahan yang sama.
2. Jelaskan kemampuan yang benar-benar dapat dicoba dan prasyaratnya.
3. Catat batasan yang masih tersisa; pindahkan fitur ke **Siap dicoba** hanya bila alur utamanya terhubung.
4. Sertakan `docs/feature-tracking.md` dalam ringkasan penutupan issue bila status tracking berubah.
