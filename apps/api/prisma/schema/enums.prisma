enum Role {
  ADMIN
  HUMAN_AGENT
}

enum AiAgentStatus {
  ACTIVE
  INACTIVE
}

enum ChannelType {
  WEB
  WHATSAPP
}

enum ChannelStatus {
  ACTIVE
  INACTIVE
}

enum WebSessionStatus {
  ACTIVE
  CLOSED
}
